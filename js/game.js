
function guu{
    var rand =Math.floor(Math.random()*Math.floor(2));
    if (rand == 0){
        alert("相手はチョキを出しました。あなたの勝ちです");
    }if (rand == 1){
        alert("相手はパーを出しました。あなたの負けです");
    }if (rand == 2){
        alert("相手はグーを出しました。あいこです");
    }
}
function tyokki{
    var rand =Math.floor(Math.random()*Math.floor(2));
    if (rand == 0){
        alert("相手はパーを出しました。あなたの勝ちです");
    }if (rand == 1){
        alert("相手はグーを出しました。あなたの負けです");
    }if (rand == 2){
        alert("相手はチョキを出しました。あいこです");
    }
}function paa{
    var rand = Math.floor(Math.random()*Math.floor(2));
    if (rand == 0){
        alert("相手はチョキを出しました。あなたの勝ちです");
    }if (rand == 1){
        alert("相手はパーを出しました。あなたの負けです");
    }if (rand == 2){
        alert("相手はグーを出しました。あいこです");
    }
}
